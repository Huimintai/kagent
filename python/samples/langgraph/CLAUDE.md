# samples/langgraph/ - LangGraph Sample Agents

| Directory | Description |
|-----------|-------------|
| `currency/` | Currency conversion agent (has `currency/` Python package) |
| `hitl-tools/` | Human-in-the-loop tools demo (has `hitl_tools/` Python package) |
| `kebab/` | Kebab ordering agent (has `kebab/` Python package) |

Each sample includes: `pyproject.toml`, `Dockerfile`, `agent.yaml`.
