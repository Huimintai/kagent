# python/samples/sota/ - SOTA Adapter Sample Agents

## Samples

| Directory | Description |
|-----------|-------------|
| `claude_code_agent/` | Claude Code CLI wrapped as BYO agent via SOTA adapter |
| `codex_agent/` | OpenAI Codex CLI wrapped as BYO agent via SOTA adapter |

Each sample includes: `agent.yaml`, `pyproject.toml`, and agent source directory.
