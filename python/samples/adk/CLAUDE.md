# samples/adk/ - ADK Sample Agents

| Directory | Description |
|-----------|-------------|
| `basic/` | Minimal ADK agent (has `basic/` Python package inside) |

Each sample includes: `pyproject.toml`, `Dockerfile`, `agent.yaml`, `README.md`.
