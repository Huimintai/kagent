# kagent-adk/tests/ - Test Suite

| Directory | Purpose |
|-----------|---------|
| `fixtures/` | Test fixtures (includes `certs/` for TLS test data) |
| `unittests/` | Unit tests (includes `converters/` and `models/` subdirs) |
