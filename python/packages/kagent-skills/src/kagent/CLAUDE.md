# kagent/ (kagent-skills)

Namespace package with skills implementation and tests.

| Directory | Content |
|-----------|---------|
| `skills/` | Skill discovery, models, prompts, session, shell |
| `tests/` | Test suite (contains `unittests/` subdir) |
