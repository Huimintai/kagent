# helm/ - Kubernetes Deployment Charts

| Chart | Purpose |
|-------|---------|
| `kagent/` | Main application chart (controller, UI, PostgreSQL, RBAC, OAuth2 proxy) |
| `kagent-crds/` | CRD definitions chart (install first) |
