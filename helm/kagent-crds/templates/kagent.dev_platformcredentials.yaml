---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: platformcredentials.kagent.dev
spec:
  group: kagent.dev
  names:
    categories:
    - kagent
    kind: PlatformCredential
    listKind: PlatformCredentialList
    plural: platformcredentials
    shortNames:
    - pcred
    singular: platformcredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.platform
      name: Platform
      type: string
    - jsonPath: .spec.source.type
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: PlatformCredential defines access to an external platform (GitHub,
          Jira, Slack, K8s).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlatformCredentialSpec defines the desired state of PlatformCredential.
            properties:
              accessPolicy:
                description: AccessPolicy defines which principals may acquire tokens
                  and with what scopes.
                items:
                  description: AccessPolicyRule defines which principals can acquire
                    tokens with specific scopes.
                  properties:
                    delegation:
                      description: |-
                        Delegation specifies whether user context is required for token minting.
                        Set to "required" if the credential must be issued on behalf of a specific user.
                      enum:
                      - required
                      - ""
                      type: string
                    principals:
                      description: |-
                        Principals lists the identities allowed to acquire tokens.
                        Format: "agent:<name>", "user:<name>", "skill:<name>", or wildcards like "agent:*".
                      items:
                        type: string
                      minItems: 1
                      type: array
                    scopes:
                      description: Scopes lists the platform-specific scopes the principal
                        may request.
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - principals
                  - scopes
                  type: object
                type: array
              platform:
                description: Platform identifies the external platform this credential
                  provides access to.
                minLength: 1
                type: string
              source:
                description: Source defines how to obtain the raw credential material.
                properties:
                  secretRef:
                    description: SecretRef references a Kubernetes Secret containing
                      the credential material.
                    properties:
                      name:
                        description: Name is the name of the Secret.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  tokenEndpoint:
                    description: TokenEndpoint is the URL used to exchange credentials
                      for tokens (e.g., OAuth2 token URL).
                    type: string
                  type:
                    allOf:
                    - enum:
                      - GitHubApp
                      - OAuth2
                      - BotToken
                      - OAuth2UserDelegation
                      - ServiceAccount
                    - enum:
                      - GitHubApp
                      - OAuth2
                      - BotToken
                      - OAuth2UserDelegation
                      - ServiceAccount
                    description: Type specifies the credential mechanism.
                    type: string
                required:
                - type
                type: object
            required:
            - platform
            - source
            type: object
          status:
            description: PlatformCredentialStatus defines the observed state of PlatformCredential.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSync:
                description: LastSync records the last time the credential source
                  was successfully validated.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
