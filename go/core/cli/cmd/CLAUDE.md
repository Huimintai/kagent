# cli/cmd/

Entry points for CLI binaries.

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `kagent/` | Main binary entry point (`main.go`); wires cobra root command with subcommands for agent, mcp, platform, envdoc, and TUI chat |
