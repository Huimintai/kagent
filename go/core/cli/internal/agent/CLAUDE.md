# cli/internal/agent/

Agent project scaffolding logic for `kagent agent init`.

## Subdirectories

| Directory | Purpose |
|-----------|---------|
| `frameworks/` | Framework-specific generators (currently ADK/Python); registry via `NewGenerator(framework, language)` |
